{
	"name": "nodejs-environment-development-in-docker",
	"version": "1.7.6",
	"private": true,
	"description": "",
	"engines": {
		"node": ">=18.17.1"
	},
	"scripts": {
		"start": "npm run build && node build/index.js",
		"build": "rimraf ./build && tsc",
		"dev": "./node_modules/.bin/nodemon --inspect=0.0.0.0:5850",
		"container": "docker-compose up --build --force-recreate",
		"container:dev": "npm prune && npm install && npm run dev",
		"test": "node test.js"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/express": "^4.17.17",
		"@types/json-schema": "^7.0.13",
		"@types/pino-http": "^5.8.1",
		"nodemon": "^2.0.22",
		"pino": "^8.15.1",
		"pino-http": "^8.5.0",
		"rimraf": "^5.0.1",
		"ts-node": "^10.9.1",
		"typescript": "^5.2.2"
	},
	"dependencies": {
		"axios": "^1.5.0",
		"express": "^4.18.2",
		"uuid": "^7.0.2"
	}
}
